export default function Layout({chidren}) {
    return (
        <div>
            <h2>
                This is the layout for the /page route
            </h2>
            {chidren}
        </div>
    );
}